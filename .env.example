# Copy to .env and fill in. .env is gitignored; this file holds names only.
# Read by experiments/common/config.py:load_env(). A variable already set in the
# shell wins over the value here.

# judge_strongreject.py --judge-model gpt-*      (the default, gpt-4o-mini)
OPENAI_API_KEY=

# judge_strongreject.py --judge-model claude-*
ANTHROPIC_API_KEY=
